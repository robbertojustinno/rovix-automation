import type{Metadata}from"next";import Link from"next/link";import{LockKeyhole}from"lucide-react";export const metadata:Metadata={title:"Área do cliente — Em breve",robots:{index:false,follow:true}};export default function Cliente(){return <section className="notFound"><LockKeyhole size={52}/><span>Em breve</span><h1>Área do cliente</h1><p>Esta estrutura está preparada para receber autenticação e serviços exclusivos futuramente.</p><Link className="button" href="/">Voltar ao início</Link></section>}
