import type{Metadata}from"next";import Link from"next/link";export const metadata:Metadata={title:"TAGCheck",alternates:{canonical:"/apps/tagcheck"},robots:{index:false,follow:true}};export default function Tagcheck(){return <LegacyProduct name="TAGCheck" href="/apps/tagcheck"/>}function LegacyProduct({name,href}:{name:string;href:string}){return <section className="notFound"><span>ROVIX Apps</span><h1>{name} agora está no catálogo oficial.</h1><p>Acesse informações, recursos e condições comerciais na página atualizada.</p><Link className="button" href={href}>Abrir {name}</Link></section>}
