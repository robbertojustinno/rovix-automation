import type {MetadataRoute} from "next";import {productRepository} from "@/repositories/products";const base=process.env.NEXT_PUBLIC_SITE_URL??"https://rovixautomation.com.br";export default async function sitemap():Promise<MetadataRoute.Sitemap>{const products=await productRepository.findCatalog();return[{url:`${base}/apps`,lastModified:new Date(),changeFrequency:"weekly",priority:1},...products.filter(p=>p.status!=="Descontinuado").map(p=>({url:`${base}/apps/${p.slug}`,lastModified:new Date(p.atualizadoEm),changeFrequency:"monthly" as const,priority:p.destaque?.9:.7}))]}
